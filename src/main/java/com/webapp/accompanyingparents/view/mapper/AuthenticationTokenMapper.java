package com.webapp.accompanyingparents.view.mapper;
/*
import com.webapp.accompanyingparents.model.AuthenticationToken;
import com.webapp.accompanyingparents.view.dto.token.AuthenticationTokenDto;
import org.mapstruct.IterableMapping;
import org.mapstruct.Mapper;
import org.mapstruct.Mapping;
import org.mapstruct.ReportingPolicy;

import java.util.List;

@Mapper(componentModel = "spring", unmappedTargetPolicy = ReportingPolicy.IGNORE)
public interface AuthenticationTokenMapper {
    @Mapping(source = "id", target = "id")
    @Mapping(source = "expiredDate", target = "tokenExpiredDate")
    @Mapping(source = "tokenName", target = "name")
    AuthenticationTokenDto entityToAuthenticationTokenDto(AuthenticationToken authenticationToken);

    @IterableMapping(elementTargetType = AuthenticationTokenDto.class)
    List<AuthenticationTokenDto> fromEntityToAuthenticationTokenDtoList(List<AuthenticationToken> authenticationTokens);
}
*/
